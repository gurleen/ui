{
  "name": "hydra-tv-ui-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the @hydra-tv component libraries (design tokens, generic components, broadcast-specific components, sports-analytics components) and their demo playground.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @hydra-tv/ui && npm run build -w @hydra-tv/broadcast && npm run build -w @hydra-tv/sports",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm run dev -w playground",
    "deploy:playground": "npm run deploy -w playground",
    "publish:packages": "npm run build && npm publish -w @hydra-tv/tokens && npm publish -w @hydra-tv/ui && npm publish -w @hydra-tv/broadcast && npm publish -w @hydra-tv/sports"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
